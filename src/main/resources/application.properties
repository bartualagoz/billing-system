# ?????????????????????????????????????????????
# ? Server Configuration
# ?????????????????????????????????????????????
server.port=8082
spring.application.name=Prototype

# ?????????????????????????????????????????????
# ? Database Configuration
# ?????????????????????????????????????????????
spring.datasource.url=jdbc:postgresql://localhost:5432/prototype_db
spring.datasource.username=postgres
spring.datasource.password=admin123
spring.datasource.driver-class-name=org.postgresql.Driver

# ?????????????????????????????????????????????
# ? JPA Configuration
# ?????????????????????????????????????????????
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ?????????????????????????????????????????????
# ? JWT Configuration
# ?????????????????????????????????????????????
# Use a strong default fallback if JWT_SECRET env variable isn't set
jwt.secret=${JWT_SECRET:XyZ_!2025s3CuReKey@#}
